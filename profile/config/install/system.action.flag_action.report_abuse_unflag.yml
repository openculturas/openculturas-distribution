langcode: en
status: true
dependencies:
  config:
    - flag.flag.report_abuse
  module:
    - flag
id: flag_action.report_abuse_unflag
label: 'Report abuse withdraw'
type: node
plugin: 'flag_action:report_abuse_unflag'
configuration:
  flag_id: report_abuse
  flag_action: unflag
