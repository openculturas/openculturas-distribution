langcode: en
status: true
dependencies:
  config:
    - flag.flag.claim_ownership
  module:
    - flag
id: flag_action.claim_ownership_flag
label: 'Claim ownership'
type: node
plugin: 'flag_action:claim_ownership_flag'
configuration:
  flag_id: claim_ownership
  flag_action: flag
