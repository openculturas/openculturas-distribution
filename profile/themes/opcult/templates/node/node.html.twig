{#
  This template maps all view modes of nodes except the "full" viewmode unless there's a dedicated template.
  Consequently, any new view mode inherits the universal teaser template by default.
#}
{% extends "@opcult/node/universal-teaser.html.twig" %}
